<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myListApp.mylist">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_grocery_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/ic_grocery_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogInActivity"
            android:theme="@style/NoActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"
                    />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BoardingActivity"
           />
        <activity android:name=".MyListActivity" />
        <activity android:name=".UpdateListActivity" />
        <activity android:name=".ComparePriceActivity" />
        <activity android:name=".SignUpActivity"
            android:theme="@style/NoActionBar"/>

    </application>

</manifest>